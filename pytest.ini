[pytest]
# Disable known setuptools-registered pytest plugins that may be installed
# globally on the host and interfere with running tests in this repo.
# If you still see plugin import errors, prefer running tests inside a
# dedicated virtual environment.
addopts = -p no:pytest_ethereum -p no:web3
python_files = test_*.py *_test.py
testpaths = vault_logic_system
